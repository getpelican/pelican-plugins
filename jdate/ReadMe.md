Jdate Plugin for Pelican
========================
This plugins changes locale date to Jalali/Persian.

Usage
-----
before using this plugin, you should install `python-dateutil` and `jdatetime` modules in your environment:
    pip3 install python-dateutil jdatetime

Then just make sure your `DEFAULT_LANG` is set to `'fa'` in Pelican settings.
